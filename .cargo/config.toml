[build]
jobs = -1

[env]
RUST_BACKTRACE = "1"
# RUST_TEST_NOCAPTURE = "1"

[net]
retry = 3

[alias]
lint = "clippy --workspace --all-features --all-targets -- -D warnings"
cov = "llvm-cov --workspace --html --open"
