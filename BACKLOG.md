# Backlog

## 1. #[template("...")]

Add attribute macro that implements ToTokens for the target.

## 2. #[derive(Moxy)]

Add moxy derive macros, use meta helpers to add functionality. (ie. `#[moxy(debug, trace)]`)
